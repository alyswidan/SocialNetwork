<%= form_for(current_user.find_friend(@user.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <div><%= hidden_field_tag :user_id, @user.id %></div>
  <%= f.submit "Unfriend", class: "btn-primary " %>
<% end %>

