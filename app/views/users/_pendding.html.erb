<%= form_for(current_user.sent_requests.
              find_by(other_user_id: @user.id),
             html:{method: :delete},
             remote: true) do |f| %>
  <div><%= hidden_field_tag :other_user_id, @user.id %></div>
  <%= f.submit "pendding", class: "btn-primary" %>
<% end %>
