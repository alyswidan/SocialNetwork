<%= form_for(current_user.received_requests.build,
             remote: true) do |f| %>
  <div><%= hidden_field_tag :user_id, @user.id %></div>
  <%= f.submit "respond", class: "btn btn-primary" %>
<% end %>